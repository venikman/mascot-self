# LM Studio Configuration
# The API endpoint for your LM Studio instance or OpenAI-compatible server
LMSTUDIO_ENDPOINT=http://localhost:1234/v1

# API key for authentication (LM Studio default is "lm-studio")
LMSTUDIO_API_KEY=lm-studio

# The model identifier to use
# Examples:
# - openai/gpt-oss-20b
# - mistralai/mistral-7b-instruct
# - meta-llama/llama-2-13b-chat
LMSTUDIO_MODEL=openai/gpt-oss-20b

# Workflow Configuration
# Minimum rating (1-10) required for slogan acceptance
# Higher values require better quality but may need more iterations
MINIMUM_RATING=8

# Maximum number of refinement attempts
# The workflow will stop after this many iterations even if quality threshold is not met
MAX_ATTEMPTS=3
